import {
    convertDateString,
    isTokenExpired,
    convertToNaturalLanguage,
    convertDateInput,
    cleanDateString,
    shortenText,
} from '../src/utils/helperFunctions'

describe("Helper Functions", () => {
	test("convertDateString", () => {
		expect("dummy").toEqual("dummy")
	})
})